services:
  backend:
    image: ${BACKEND_IMAGE}
    container_name: caris-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - red-compartida

networks:
  red-compartida:
    external: true
